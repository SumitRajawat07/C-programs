#include<stdio.h>
int main()
{
    int length = 4;
    int width = 5;
    int area;

    area = length*width;

    printf("length is : %d\n",length);
    printf("width is : %d\n", width);
    printf("area of rectangle is : %d",area);

    return 0;
}